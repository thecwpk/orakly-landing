/** Minimal routes for standalone marketing site (app-shell navbar uses a subset). */
export const ROUTES = {
  home: "/",
  /** Markets directory (marketing shell). */
  discover: "/#live-markets",
  signIn: "/#early-access",
  dapp: "/",
} as const;
